#ifndef GLOBALE_H // Si la constante n'a pas été définie le fichier n'a jamais été inclus
#define GLOBALE_H // On définit la constante pour que la prochaine fois le fichier ne soit plus inclus

#define MAX_SYMBOLES 256
unsigned int NbSymboles=0;   //va compter le nb de de feuilles de l'arbre pour ensuite creer pour l'entete du fichier binaire
unsigned int MaxNbBits=0; //inutile

#endif